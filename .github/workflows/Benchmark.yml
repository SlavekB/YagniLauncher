name: Benchmark

on:
  push:
    branches:
      - master
    paths:
      - 'domain/grid/**'
      - 'build-logic/**'
      - 'gradle/**'
      - '.github/workflows/Benchmark.yml'
  pull_request:
    paths:
      - 'domain/grid/**'
      - 'build-logic/**'
      - 'gradle/**'
      - '.github/workflows/Benchmark.yml'
  workflow_dispatch:

concurrency:
  group: benchmark-${{ github.ref }}
  cancel-in-progress: true

jobs:
  run-benchmarks:
    name: "Run Benchmarks"
    runs-on: ubuntu-latest
    timeout-minutes: 30

    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Grant execute permission to Gradle wrapper
        run: chmod +x ./gradlew

      - name: Copy CI gradle.properties
        run: mkdir -p ~/.gradle ; cp .github/ci-gradle.properties ~/.gradle/gradle.properties

      - name: Setup JDK 17
        uses: actions/setup-java@v6
        with:
          distribution: 'zulu'
          java-version: 17

      - name: Setup Gradle for untrusted runs
        if: ${{ github.event_name != 'push' || github.ref != 'refs/heads/master' }}
        uses: gradle/actions/setup-gradle@v5

      - name: Setup Gradle for trusted master pushes
        if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/master' }}
        uses: gradle/actions/setup-gradle@v5
        with:
          cache-encryption-key: ${{ secrets.GRADLE_ENCRYPTION_KEY }}

      - name: Run benchmarks
        run: ./gradlew benchmark

      - name: Upload benchmark results
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v7
        with:
          name: benchmark-results
          path: '**/build/reports/benchmarks/main/**/benchmark.json'
          retention-days: 90

  post-benchmark-results:
    name: "Post Benchmark Results"
    needs: run-benchmarks
    if: ${{ needs.run-benchmarks.result == 'success' && github.event_name == 'pull_request' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Download benchmark results
        uses: actions/download-artifact@v8
        with:
          name: benchmark-results
          path: benchmark-results

      - name: Generate PR comment body
        run: |
          echo "## 📊 Benchmark Results" > benchmark-comment.md
          echo "" >> benchmark-comment.md
          echo "| Module | Benchmark | itemCount | Score | Error | Unit |" >> benchmark-comment.md
          echo "|---|---|---|---|---|---|" >> benchmark-comment.md
          for f in $(find benchmark-results -name "benchmark.json"); do
            module=$(echo "$f" | sed -E 's|^benchmark-results/([^/]+(/[^/]+)*)/build/reports/benchmarks/.*|\1|')
            jq -r --arg module "$module" '.[] | "| \($module) | \(.benchmark | split(".") | last) | \(.params.itemCount) | \(.primaryMetric.score | (. * 1000 | round) / 1000) | ±\(.primaryMetric.scoreError | (. * 1000 | round) / 1000) | \(.primaryMetric.scoreUnit) |"' "$f" >> benchmark-comment.md
          done

      - name: Post PR comment
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: benchmark-results
          path: benchmark-comment.md
